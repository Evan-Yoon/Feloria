import Phaser from 'phaser';

export class CodexScene extends Phaser.Scene {
  constructor() {
    super({ key: 'CodexScene' });
  }

  create() {
    console.log('CodexScene placeholder loaded');
  }
}
