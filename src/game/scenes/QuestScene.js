import Phaser from 'phaser';

export class QuestScene extends Phaser.Scene {
  constructor() {
    super({ key: 'QuestScene' });
  }

  create() {
    console.log('QuestScene placeholder loaded');
  }
}
