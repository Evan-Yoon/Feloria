import Phaser from 'phaser';

export class EvolutionScene extends Phaser.Scene {
  constructor() {
    super({ key: 'EvolutionScene' });
  }

  create() {
    console.log('EvolutionScene placeholder loaded');
  }
}
