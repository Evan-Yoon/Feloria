import Phaser from 'phaser';

export class PartyScene extends Phaser.Scene {
  constructor() {
    super({ key: 'PartyScene' });
  }

  create() {
    console.log('PartyScene placeholder loaded');
  }
}
