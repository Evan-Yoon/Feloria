/**
 * mapLoader.js - Responsible for loading and parsing Tiled maps.
 */
export const mapLoader = {
    loadMap: (scene, mapKey) => {
        console.log(`MapLoader: Loading map ${mapKey}`);
    }
};
